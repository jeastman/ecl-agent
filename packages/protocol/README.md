# Protocol Package

Owns JSON-RPC envelopes, transport-neutral runtime payloads, Milestone 2 task/approval/inspection method models, and runtime event envelopes. The package is the shared contract between the authoritative runtime and thin clients; it does not contain orchestration logic or framework-specific types.
