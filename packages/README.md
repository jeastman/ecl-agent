# Shared Packages

These packages form the shared kernel for the local agent harness. App code imports them directly rather than redefining schemas in the CLI or runtime.

Development workflows for these packages are run from the repository root with `uv run poe ...`.
