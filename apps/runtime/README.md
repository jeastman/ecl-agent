# Runtime App

The runtime is the system of record for task execution. The implemented Milestone 2 baseline provides protocol-backed task creation and inspection, persisted event replay, governed sandbox access, runtime-owned artifact registration, durable memory and diagnostics, approval-aware pause/resume, restart recovery, and a real single-agent DeepAgent-backed harness path over JSON-RPC stdio.
