# Runtime App

The runtime is the system of record for task execution. Milestone 1 Phase 1 provides protocol-backed task creation, task inspection, event replay, artifact listing seams, and a stubbed runtime-owned orchestration path over JSON-RPC stdio.
