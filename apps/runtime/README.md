# Runtime App

The runtime is the system of record for task execution. Milestone 1 currently provides protocol-backed task creation, task inspection, event replay, governed sandbox access, runtime-owned artifact registration, and a stubbed orchestration path over JSON-RPC stdio.
