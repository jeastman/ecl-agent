# Apps

- `cli` is a thin client that speaks the shared JSON-RPC contract.
- `runtime` is the authoritative execution host, even in stub form.
- Root repo tooling is managed from `/Users/jeastman/Projects/e/ecl-agent/pyproject.toml` via `uv` and `poethepoet`.
